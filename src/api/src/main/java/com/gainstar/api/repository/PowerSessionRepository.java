package com.gainstar.api.repository;

import com.gainstar.api.entity.action.PowerSession;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PowerSessionRepository extends JpaRepository<PowerSession, Long> {
}
